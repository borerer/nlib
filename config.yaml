log-level: debug
api:
  addr: 0.0.0.0
  port: 9502
builtin:
  echo:
    enabled: true
  kv:
    enabled: true
    mongo:
      uri:
      database: nlib
      collection: kv
  logs:
    enabled: true
    mongo:
      uri:
      database: nlib
      collection: logs
  files:
    enabled: true
    minio:
      enabled: true
      endpoint:
      access-key:
      secret-key:
      use-ssl: true
      bucket: nlib
    webdav:
      enabled: true
    samba:
      enabled: true
